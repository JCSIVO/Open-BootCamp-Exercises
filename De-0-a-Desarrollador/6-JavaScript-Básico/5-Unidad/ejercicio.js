let altura_cm = 173;
let altura_m = 1.73;
let peso_dec = 80.24
let altura_redon = Math.ceil(altura_m)
console.log(altura_redon)
let peso_redon = Math.floor(peso_dec)
console.log(peso_redon)

let igual = Number.MAX_VALUE + 1 === Number.MAX_VALUE
console.log(igual)