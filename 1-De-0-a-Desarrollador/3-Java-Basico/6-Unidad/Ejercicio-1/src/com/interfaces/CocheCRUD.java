package com.interfaces;

//Declaracion de los metodos y no se implementan
//Dice lo que hay  que hacer pero no lo hace, solo lo indica

public interface CocheCRUD {
    void save();
    void findAll();
    void delete();
}
